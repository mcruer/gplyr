% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulate.R
\name{left_join_unique}
\alias{left_join_unique}
\title{Left Join Without Column Name Collisions}
\usage{
left_join_unique(df1, df2, by, quiet = FALSE)
}
\arguments{
\item{df1}{A data frame or tibble. The left-hand side of the join.}

\item{df2}{A data frame or tibble. The right-hand side of the join. Any columns that also
exist in \code{df1} (but are not part of the join keys) will be removed before the join.}

\item{by}{A character vector specifying the column(s) to join by. Passed directly to \code{left_join()}.}

\item{quiet}{Logical. If \code{FALSE} (default), prints informative messages about the join and dropped columns.}
}
\value{
A tibble resulting from the join, without conflicting column names.
}
\description{
Performs a left join between two data frames while automatically removing overlapping columns
from the second data frame (excluding those used for joining), to avoid \code{.x} / \code{.y} suffixes.
}
\examples{
\dontrun{
df1 <- tibble(id = 1:3, value = c("A", "B", "C"))
df2 <- tibble(id = 2:4, value = c("X", "Y", "Z"), extra = c(10, 20, 30))

left_join_unique(df1, df2, by = "id")
}
}
